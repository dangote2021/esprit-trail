// DesktopNav désactivé — la BottomNav est affichée sur toutes les tailles d'écran.
// UX mobile-first : la nav du bas reste la référence, plus accessible au pouce.
export default function DesktopNav() {
  return null;
}
